export type articleProps = {
  index: number;
  headline: string;
  temporaryContent: string;
  section: string;
  author: string;
  link: string;
};

export type articleTitleProps = {
  title: string;
};
